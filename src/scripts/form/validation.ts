export function validateForm(): boolean {
     const isValid = true;

     const allFormGroups = document?.querySelectorAll('#transaction-form .form-group');

     allFormGroups.forEach(() => {});

     return isValid;
}
